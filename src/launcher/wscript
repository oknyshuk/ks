#! /usr/bin/env python


def options(opt):
    return


def configure(conf):
    conf.define("LAUNCHERONLY", True)


def build(bld):
    libs = [
        "tier0",
        "memoverride",
        "appframework",
        "tier1",
        "tier2",
        "tier3",
        "interfaces",
        "vstdlib",
        "SDL3",
    ]

    includes = []

    source = [
        "launcher.cpp",
        "reslistgenerator.cpp",
    ]

    bld.objects(
        source=source,
        target="launcher",
        features="cxx",
        includes=includes,
        use=libs,
        idx=bld.get_taskgen_count(),
    )
