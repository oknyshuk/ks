#! /usr/bin/env python


def options(opt):
    return


def configure(conf):
    return


def build(bld):
    libs = [
        "tier0",
        "memoverride",
        "tier1",
        "tier2",
        "interfaces",
        "vstdlib",
        "mathlib",
        "shaderlib",
    ]

    includes = [".."]

    source = ["shaderapiempty.cpp"]

    bld.objects(
        source=source,
        target="shaderapiempty",
        features="cxx",
        includes=includes,
        use=libs,
        idx=bld.get_taskgen_count(),
    )
