#! /usr/bin/env python


def options(opt):
    return


def configure(conf):
    return


def build(bld):
    libs = ["tier0", "memoverride", "tier1", "interfaces", "vstdlib"]

    includes = ["../game/shared"]

    source = [
        "SceneFileCache.cpp",
    ]

    bld.objects(
        source=source,
        target="scenefilecache",
        features="cxx",
        includes=includes,
        use=libs,
        idx=bld.get_taskgen_count(),
    )
